package toleco.controller;

import junit.framework.TestCase;

/**
 * Unit Test for GameState
 * @author adam
 */
public class GameStateTest extends TestCase
{

    public GameStateTest() {
    }

    public void testToString()
    {
        assertEquals("kTerrainSelected", GameState.kTerrainSelected.toString());
        assertEquals("kAttackMode", GameState.kAttackMode.toString());
        assertEquals("kGameOver", GameState.kGameOver.toString());
        assertEquals("kNoneSelected", GameState.kNoneSelected.toString());
    }
}