package toleco.terrain;

/**
* This Terrain is a default terrain only to be used when TerrainFactory cannot find a
* terrain it has been asked to build. It will be unable to find a terrain either
* because the terrain is in a map but not in the Terrain folder, or the file in the
* Terrain folder is malformed.
*
* @author Jon Moorman
* @version 1.0
*/
public class DefaultTerrain extends Terrain
{
    /**
    * The constructor for a DefaultTerrain.
    * @param xCoord The x-coordinate where the terrain will be located
    * @param yCoord The y-coordinate where the terrain will be located
    */
    public DefaultTerrain(int xCoord, int yCoord)
    {
        super("Default", 0, 0, xCoord, yCoord);
    }
}
